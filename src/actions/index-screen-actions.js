import * as types from './action-types';

var homeScreen = {
    type: types.INDEX_HOME_SCREEN
}

var studentScreen = {
    type: types.INDEX_STUDENT_SCREEN
}

var lecturerScreen = {
    type: types.INDEX_LECTURER_SCREEN
}   

var studentIDScreen = {
    type: types.INDEX_STUDENTID_SCREEN
}

export default {
    homeScreen,
    studentScreen,
    lecturerScreen,
    studentIDScreen,
}